youtube-transcript-api
requests
python-slugify
yt-dlp
google-api-python-client
google-auth
google-auth-oauthlib
google-genai
openai
pillow
anthropic
pytest
flask
python-dotenv
playwright
faster-whisper
defusedxml
python-docx
pdfplumber

# src/shorts/heygen_renderer.py — remove_background() real background
# removal for HeyGen avatar footage (workaround for the Digital Twin
# avatar type silently ignoring HeyGen's own `background` API param).
# GPU build installed but CUDA does NOT currently engage on this machine
# (missing system CUDA Toolkit runtime, e.g. cublasLt64_13.dll — separate
# from these pip packages) — falls back to CPU automatically, still
# functional, just slower. See remove_background()'s docstring for real
# benchmarked runtime.
rembg
onnxruntime-gpu

# System dependency (not pip): ffmpeg — required by skool-archive to merge
# video+audio (Loom/YouTube 1080p). Install on Windows with:
#   winget install Gyan.FFmpeg
# The archiver auto-detects it on PATH or in the winget install dir.
